import React, { useCallback, useMemo, useRef } from 'react'
import { View, Text, StyleSheet, Button } from 'react-native'
import {
  BottomSheetModal,
  BottomSheetModalProvider,
} from '@gorhom/bottom-sheet'

const SoundPlayFacePlate = () => {}
